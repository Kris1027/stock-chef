import IngredientList from '@/app/_components/ingredient-list';

const HomePage = async () => {
    return (
        <main>
            <IngredientList />
        </main>
    );
};

export default HomePage;
